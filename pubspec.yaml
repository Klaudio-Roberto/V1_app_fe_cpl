name: v1_app_fe_cpl
description: Um projeto Flutter para gerenciar itens com SQFlite.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Use a versão do seu SDK Flutter

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0 # Versão estável mais recente
  path_provider: ^2.0.11 # Para encontrar o caminho do banco de dados no dispositivo
  path: ^1.8.3 # Para manipular caminhos de arquivos
 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0 # Boas práticas de linting

flutter:
  uses-material-design: true